# Heavy driver needed ONLY for the live Snowflake load + the gated integration tests
# (tests/integration/test_snowflake_load.py). Kept out of requirements-ci.txt on purpose:
# the unit tests import it lazily / via importorskip, and CI has no Snowflake creds, so
# installing it there would just slow CI without running anything. Install in the cloud
# session: pip install -r requirements-snowflake.txt
snowflake-connector-python==4.6.0
dbt-snowflake==1.11.6
# Python 3.14 only: dbt-adapters pins mashumaro<3.18, which cannot serialize Optional[]
# fields on 3.14. Pinning mashumaro>=3.22 HERE would make this file ResolutionImpossible,
# so on 3.14 run a second, separate step after installing this file (pip warns about the
# dbt-adapters pin; the warning is harmless):
#   pip install -U 'mashumaro>=3.22'
# Pythons 3.11-3.13 need nothing extra. Remove once dbt-adapters raises its upper bound.

